import axios from 'axios';
import { ZujuanUrl } from './url';

export class Zujuan {
    url: ZujuanUrl = new ZujuanUrl();
    /**
     * 登录
     * @param username 用户名
     * @param password 密码
     * @param service 地址
     */
    login(username: string, password: string, service: string) {
        
    }
}